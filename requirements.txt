# Runtime dependencies for toxic_comment_classifier.
#
# This file is the install surface for `pip install -r requirements.txt`,
# Docker builds, and CI. It tracks only DIRECT dependencies — pip resolves
# transitive ones at install time. `pyproject.toml` is the source of truth;
# keep this file in sync with the `[project].dependencies` table.
#
# Dev tooling (pytest, ruff, mypy, pre-commit, memory-profiler) lives in
# `requirements_dev.txt` and is installed via `make dev` or
# `pip install -e .[dev]`.

# --- Core ML stack ----------------------------------------------------------
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0
joblib>=1.4.0
matplotlib>=3.9.0
tqdm>=4.66.0
pyyaml>=6.0

# --- Configuration & experiment tracking -----------------------------------
hydra-core>=1.3.0
omegaconf>=2.3.0
mlflow>=2.16.0
rich>=13.0.0

# --- Data versioning -------------------------------------------------------
dvc>=3.55.0
dvc-gdrive>=3.0.0

# --- Phase 3 roadmap (transformer fine-tune + serving) ---------------------
# Kept in runtime deps so the Docker image built today can fine-tune
# DistilBERT and serve the model via FastAPI once that code lands.
torch>=2.3.0
transformers>=4.40.0
datasets>=2.20.0
fastapi>=0.111.0
uvicorn>=0.30.0
