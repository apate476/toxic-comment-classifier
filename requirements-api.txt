# Slim dependency set for the serving container (dockerfiles/Dockerfile.api).
# Deliberately excludes the training stack (torch, mlflow, dvc, hydra) to
# keep the Cloud Run image small and cold starts fast.
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
scikit-learn>=1.5.0
joblib>=1.4.0
numpy>=1.26.0
