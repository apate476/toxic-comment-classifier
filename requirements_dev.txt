# Development-only dependencies for toxic_comment_classifier.
#
# Install with:
#     pip install -r requirements.txt -r requirements_dev.txt
# or, preferably, via the editable extras group:
#     pip install -e .[dev]
#
# Mirrors `[project.optional-dependencies].dev` in pyproject.toml.

# --- Test + coverage -------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0

# --- Lint, type-check, hooks -----------------------------------------------
ruff>=0.6.0
mypy>=1.11
pre-commit>=3.8

# --- Profiling (Phase 2 reporting) -----------------------------------------
memory-profiler>=0.61
# Framework profiler that attributes C-extension time (sklearn / NumPy)
# to the originating Python line. Required for the classical-ML rubric.
scalene>=1.5.40
